# Bắt buộc
mediapipe==1.0.0
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.0
faster-whisper>=1.0.0

# Tùy chọn nhưng khuyến nghị mạnh (tách nhạc nền khỏi giọng nói).
# Nặng (~2-3GB do kéo theo torch) — bỏ comment dòng dưới nếu muốn cài,
# hoặc cài riêng: pip install demucs
# demucs>=4.0.0
